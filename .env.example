# Local dev uses the Vite proxy in vite.config.js, so this can stay empty.
# Set it only when the frontend is served without that proxy.
# Example: VITE_API_BASE_URL=http://localhost:8000
VITE_API_BASE_URL=
